{
  "name": "gmail-webhook-processor",
  "version": "1.0.0",
  "description": "Gmail push notifications → data extraction → webhook forwarding",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "get-token": "node scripts/get-refresh-token.js",
    "register-watch": "node scripts/register-watch.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^140.0.0"
  }
}
